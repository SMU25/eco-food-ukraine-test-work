import { ButtonVariants } from "./types";

export const BUTTON_STYLE_VARIANTS = {
  [ButtonVariants.PRIMARY]:
    "bg-yellow-light py-2.5 hover:bg-black hover:text-white active:brightness-95",
  [ButtonVariants.SECONDARY]:
    "bg-gray-light py-2.75 hover:brightness-90 active:brightness-75",
};
